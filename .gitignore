# macOS
.DS_Store

# Student PII — rosters must never enter version control
next_class.csv
*roster*.csv

# Firebase config / credentials
GoogleService-Info.plist
*-serviceaccount.json
serviceAccount*.json
.env
.env.local
.env.*.local
*.pem
*.p8

# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
build/
*.xcarchive

# The admin dashboard is its own repository (FlockIn_adminDashboard) and holds
# .env.local with the Firebase service account private key. This repo is PUBLIC
# — the App Store support and privacy-policy URLs point at it — so the whole
# directory stays out, node_modules and all.
admin-dashboard/
